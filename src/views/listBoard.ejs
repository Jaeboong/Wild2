<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>민원 게시판</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>민원 게시판</h1>
  <a href="/board/create">글 작성</a>
  <a href="/board/logout">로그아웃</a>
  <table>
    <thead>
      <tr>
        <th>작성자</th>
        <th>제목</th>
        <th>추천</th>
      </tr>
    </thead>
    <tbody>
      <% posts.forEach((post, index) => { %>
        <tr>
          <td><%= post.author %></td>
          <td><a href="/board/view/<%= post.id %>"><%= post.title %></a></td>
          <td><%= post.recommendations %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>
