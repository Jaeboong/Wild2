<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title><%= post.title %></title>
</head>
<body>
    <h1><%= post.title %></h1>
    <p><%= post.content %></p>
    <p>작성자: <%= post.authorName %></p>
    <p>작성일: <%= post.date %></p>
    <p>추천 수: <%= post.recommend %></p>
    <p>신고 수: <%= post.reports %></p>

    <h2>댓글</h2>
    <ul>
        <% comments.forEach(comment => { %>
            <li>
                <p>작성자: <%= comment.authorName %> 내용: <%= comment.comment %></p>
            </li>
        <% }); %>
    </ul>

    <form action="/board/comment/<%= post.id %>" method="post">
        <textarea name="comment" placeholder="댓글을 입력하세요" required></textarea>
        <button type="submit">댓글 작성</button>
    </form>
</body>
</html>
