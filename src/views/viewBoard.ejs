<!DOCTYPE html>
<html>
<head>
  <title><%= post.title %></title>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
  <h1><%= post.title %></h1>
  <p>작성자: <%= post.author %></p>
  <p><%= post.content %></p>
  <p>찬성 투표 수: <%= post.votes.for %> | 반대 투표 수: <%= post.votes.against %></p>
  <p>추천 수: <%= post.recommendations %></p>

  <% if (!hasVoted) { %>
  <form action="/board/vote/<%= post.id %>" method="POST">
    <button type="submit" name="voteType" value="for">찬성 투표</button>
    <button type="submit" name="voteType" value="against">반대 투표</button>
  </form>
  <% } %>

  <% if (!hasRecommended) { %>
  <form action="/board/recommend/<%= post.id %>" method="POST">
    <button type="submit">추천</button>
  </form>
  <% } %>

  <h2>투표 현황</h2>
  <canvas id="voteChart" width="400" height="200"></canvas>
  <script>
    const ctx = document.getElementById('voteChart').getContext('2d');
    const voteChart = new Chart(ctx, {
      type: 'bar',
      data: {
        labels: ['찬성', '반대'],
        datasets: [{
          label: '투표 수',
          data: [<%= post.votes.for %>, <%= post.votes.against %>],
          backgroundColor: [
            'rgba(75, 192, 192, 0.2)',
            'rgba(255, 99, 132, 0.2)'
          ],
          borderColor: [
            'rgba(75, 192, 192, 1)',
            'rgba(255, 99, 132, 1)'
          ],
          borderWidth: 1
        }]
      },
      options: {
        scales: {
          y: {
            beginAtZero: true
          }
        }
      }
    });
  </script>

  <h2>댓글</h2>
  <ul>
    <% post.comments.forEach(comment => { %>
      <li>
        <strong><%= comment.author %>:</strong> <%= comment.content %>
        <% if (!commentRecommendations[comment.id]) { %>
        <form action="/board/comment/recommend/<%= post.id %>/<%= comment.id %>" method="POST" style="display:inline;">
          <button type="submit">추천 (<%= comment.recommendations %>)</button>
        </form>
        <% } else { %>
        <span>추천 (<%= comment.recommendations %>)</span>
        <% } %>
      </li>
    <% }) %>
  </ul>

  <form action="/board/comment/<%= post.id %>" method="POST">
    <input type="text" name="author" placeholder="이름" required />
    <textarea name="comment" placeholder="댓글을 입력하세요" required></textarea>
    <button type="submit">댓글 작성</button>
    
  </form>

  <a href="/board/list">
    <button type="button">글목록</button>
  </a>
</body>
</html>
